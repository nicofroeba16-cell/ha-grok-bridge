# Migration to 1.0.0

The bridge now supports bidirectional `/config` synchronization, conflict detection, snapshots and restore, dry-run, configurable exclusions, secret scanning, initial sync direction, manual Ingress operations, health/status, locking, deletion handling, and opt-in history cleanup.
