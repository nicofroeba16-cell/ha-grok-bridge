Welle 1 auf der HA-Box — kein Script-Start noetig.

Nach ki2 inactive:
1. Einstellungen → Add-ons → HA Grok Bridge → Start
2. Log:
   HA Grok Bridge Add-on 0.2.4
   /config ok
   Git-SSH-Key: /ssl/ha-grok-bridge
   first boot: arm last_id=ha-20260901-020100-r8k3, skip exec

Nicht: zweiten Poller anlassen. Nicht command.json aendern.
Rollback: Add-on Stop, auf ki2 systemctl enable --now ha-grok-bridge.service
