# 1.13

- Fix AI-control `sync`: an explicit `sync` command now performs a real GitHub → `/config` deployment instead of only reporting the current commit/hash.
- The normal bidirectional sync behavior remains unchanged.
