configuration:
  poll_interval:
    name: Poll interval
    description: Seconds between synchronization checks
  config_repo:
    name: Git repository
    description: SSH URL of the configuration repository
