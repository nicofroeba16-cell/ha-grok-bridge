configuration:
  poll_interval:
    name: Poll-Intervall
    description: Sekunden zwischen den Checks von command.json
  git_repo:
    name: Git-Remote
    description: SSH-URL des Bridge-Repos (command.json / result.json)
